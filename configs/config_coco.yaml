data:
  dataset: "coco"
  images_path: "datasets/coco"
  captions_path: "datasets/coco/caption_datasets/dataset_coco.json"

loss:
  cls_label_path: "datasets/coco/coco_multilabel_map.pt"

paraphraser:
  # Used only when paraphraser.type == "llm_precomputed" (set by run registry).
  precomputed_path: "datasets/coco/caption_rewrites_mistral-7b-instruct-v0.2.json"

