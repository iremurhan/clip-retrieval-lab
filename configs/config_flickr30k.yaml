data:
  dataset: "flickr30k"
  images_path: "datasets/flickr30k/flickr30k_images"
  captions_path: "datasets/flickr30k/caption_datasets/dataset_flickr30k.json"
  # Used only by B5_seg (gated on model.seg_embed_size). Path is relative to /workspace.
  seg_map_dir: "datasets/flickr30k/sam_masks"
